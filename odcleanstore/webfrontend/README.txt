================================================================================
ODCS - Web Frontend
================================================================================

To build and run the application, use the following command (executed in the
project root directory, e.g. where the corresponding pom.xml file resides):

mvn clean package jetty:run

Maven should build the war file and launch it via an instance of the Jetty
application server on localhost, port 8080.
